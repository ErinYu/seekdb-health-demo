# SeekDB connection (matches docker-compose.yml defaults)
SEEKDB_HOST=127.0.0.1
SEEKDB_PORT=2881
SEEKDB_USER=root
SEEKDB_PASSWORD=
SEEKDB_DATABASE=health_demo

# Anthropic API key (optional — LLM analysis is skipped if not set)
ANTHROPIC_API_KEY=sk-ant-...
